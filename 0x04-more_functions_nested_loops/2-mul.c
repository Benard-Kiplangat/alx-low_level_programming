#include "main.h"

/**
 * mul - multiplies two numbers.
 * @a: first parameter
 * @b: second parameter
 * Return: Always 0.
 */

int mul(int a, int b)
{
int result;

result = a * b;

return (result);
}
