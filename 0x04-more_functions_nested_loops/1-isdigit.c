#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 1` if c is upper, otherwise 0.
 */
int _isdigit(int c)
{
	int isdig;
	int isdigit(int a);
	if (isdigit(c))
		isdig = 1;
	else
		isdig = 0;
	return (isdig);
}
