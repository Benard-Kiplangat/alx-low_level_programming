#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 1` if c is upper, otherwise 0.
 */
int _isupper(int c)
{
	int isup;
	int isupper(int a);
	if (isupper(c))
		isup = 1;
	else
		isup = 0;
	return (isup);
}
