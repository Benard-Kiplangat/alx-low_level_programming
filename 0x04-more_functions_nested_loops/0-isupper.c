#include "main.h"

/**
 * _isupper - check the code if a letter is upper or lower.
 *@c: first parameter
 * Return: Always 1` if c is upper, otherwise 0.
 */
int _isupper(int c)
{
	int isup;

	int isupper(int a);

	if (isupper(c))
		isup = 1;
	else
		isup = 0;

	return (isup);
}
